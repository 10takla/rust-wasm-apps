use crate::planet::shared::point::Point;

use super::Plane;

// impl<T> From<Plane<T>> for [Point<T>; 4] {
//     fn from(value: Plane<T>) -> Self {
//         let (a_points, b_points): ([Point<T>; 3], [Point<T>; 3]) = (value.a.into(), value.b.into());

//     }
// }